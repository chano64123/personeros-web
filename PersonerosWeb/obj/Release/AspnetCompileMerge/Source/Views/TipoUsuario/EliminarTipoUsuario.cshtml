@using PersonerosWeb.Models;
@model TipoUsuario
@{
    Layout = null;
}

@using(Html.BeginForm("EliminarTipoUsuario", "TipoUsuario", FormMethod.Post)) {
    @Html.HiddenFor(m => m.idTipoUsuario)
    <div class="card">
        <div class="card-body">
            <p>¿Seguro que deseas eliminar el tipo de usuario <span class="fw-bold">@Model.nombre</span>?</p>
            <p class="fst-italic">*Se borrarán todos los datos asociados al tipo de usuario.</p>
            <div class="row">
                <div class="col d-grid">
                    <button data-bs-dismiss="modal" type="submit" class="btn btn-primary btn-lg">Sí</button>
                </div>
                <div class="col d-grid">
                    <a data-bs-dismiss="modal" class="btn btn-danger btn-lg">Cancelar</a>
                </div>
            </div>
        </div>
    </div>
}